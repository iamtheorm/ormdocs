import ContentWrapper from "../components/ContentWrapper";

export default function Page() {
  const content = "\n            <header className=\"article-header\">\n                <div className=\"dossier-banner\">\n                    <span>Operations Manual / Infrastructure</span>\n                    <span>Audience / SRE & DevOps</span>\n                    <span>Status / Active Runbook</span>\n                </div>\n                <h2>The Cloud Infrastructure Runbook (AWS)</h2>\n                <p className=\"article-subtitle\">Highly Available IoT Energy Optimization Platform</p>\n            </header>\n\n            <div className=\"article-body\">\n                <section className=\"prose-section\" id=\"section-1\">\n                    <div className=\"section-marker\">01</div>\n                    <div className=\"section-content\">\n                        <p className=\"mini-label\">01 / Overview</p>\n                        <h3>Introduction to the Operational Blueprint</h3>\n\n                        <div className=\"admonition note\">\n                            <div className=\"admonition-header\">\n                                <svg width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><circle cx=\"12\" cy=\"12\" r=\"10\"></circle><line x1=\"12\" y1=\"16\" x2=\"12\" y2=\"12\"></line><line x1=\"12\" y1=\"8\" x2=\"12.01\" y2=\"8\"></line></svg>\n                                Note\n                            </div>\n                            <p>This section outlines key architectural decisions. Keep in mind that system constraints may dictate deviations from these optimal pathways depending on your deployment environment.</p>\n                        </div>\n                        <p>Operating a highly available, scalable internet of things (IoT) energy optimization platform on Amazon Web Services (AWS) requires strict adherence to the AWS Well-Architected Framework. The deployment of an enterprise-grade IoT platform—responsible for ingesting millions of telemetry records from distributed energy assets, processing them in real-time, and executing automated control algorithms—demands an architecture that prioritizes fault isolation, data durability, and zero-downtime continuous integration and continuous deployment (CI/CD).</p>\n                        <p>This operations manual serves as the definitive runbook for Site Reliability Engineering (SRE) and DevOps teams. A standard runbook must provide actionable, step-by-step operational guidance for handling recurring tasks, alert remediation, and deployment procedures, ensuring that engineers have the necessary context to prevent destructive actions without being overwhelmed by extraneous theory. To satisfy these requirements, this document exhaustively details the reference architecture, network topology, minimum-privilege security guardrails, edge-device resilience strategies, database migration methodologies, and explicit deployment rollback procedures.</p>\n                        <p>By utilizing infrastructure as code (IaC) and serverless components, the architecture inherently aligns with the Reliability and Operational Excellence pillars of the Well-Architected Framework, effectively reducing toil and standardizing failure management across the fleet.</p>\n                    </div>\n                </section>\n\n                <section className=\"prose-section\" id=\"section-2\">\n                    <div className=\"section-marker\">02</div>\n                    <div className=\"section-content\">\n                        <p className=\"mini-label\">02 / Topology</p>\n                        <h3>Reference Architecture and Network Topology</h3>\n\n                        <div className=\"admonition tip\">\n                            <div className=\"admonition-header\">\n                                <svg width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><path d=\"M12 2v20M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6\"></path></svg>\n                                Tip\n                            </div>\n                            <p>To optimize performance and avoid unnecessary bottlenecks, ensure your caching layer is properly invalidating outdated tokens when using this workflow in production.</p>\n                        </div>\n                        <p>The foundation of a resilient cloud environment is a rigorously partitioned Virtual Private Cloud (VPC) topology that enforces cross-Availability Zone (AZ) high availability and strict ingress/egress controls. The platform utilizes a multi-AZ deployment strategy across a minimum of three isolated Availability Zones to guarantee fault tolerance and 99.999999999% durability for object storage systems.</p>\n                        \n                        <h4>The Three-Tier VPC Architecture</h4>\n                        <p>The network is logically segmented into three distinct subnet tiers, each serving a specific security boundary and workload function:</p>\n                        <ul>\n                            <li><strong>Public Ingress Tier:</strong> This tier hosts the Application Load Balancers (ALB) and NAT Gateways. It is the only subnet equipped with a default route to an Internet Gateway (IGW). External traffic from web dashboards and operator APIs enters exclusively through the ALBs, which terminate TLS connections and forward traffic to the internal compute tier.</li>\n                            <li><strong>Private Compute Tier:</strong> This tier hosts the Amazon Elastic Container Service (ECS) Fargate tasks, internal microservices, and AWS Lambda functions responsible for telemetry enrichment. Resources in this tier have no direct internet access; outbound requests (e.g., reaching third-party weather APIs for energy forecasting) are routed strictly through the NAT Gateways residing in the public tier.</li>\n                            <li><strong>Isolated Data Tier:</strong> The lowest level of the network topology contains the persistent datastores, primarily Amazon Aurora Serverless v2 clusters and Amazon ElastiCache instances. This tier is intentionally designed without any outbound route to the internet, providing a logical air-gap that prevents data exfiltration even if the compute tier is compromised.</li>\n                        </ul>\n\n                        <h4>Service Integration Flow</h4>\n                        <p>The integration between the edge devices, the serverless ingestion pipeline, and the synchronous application programming interfaces (APIs) forms a continuous, event-driven ecosystem.</p>\n                        \n                        <div className=\"mermaid\">\ngraph LR\n    subgraph AWS[\"AWS Cloud Architecture\"]\n        subgraph MultiAZ[\"Multi-AZ Region\"]\n            IGW[\"Internet Gateway\"] --> ALB[\"Application Load Balancer\"]\n            \n            subgraph PublicTier[\"Public Tier\"]\n                ALB\n                NAT[\"NAT Gateways\"]\n            end\n            \n            subgraph PrivateTier[\"Private Compute Tier\"]\n                ECS[\"ECS Fargate API Services\"]\n                Lambda[\"Data Enrichment Lambda\"]\n            end\n            \n            subgraph DataTier[\"Isolated Data Tier\"]\n                Aurora[(\"Aurora Serverless v2 PostgreSQL\")]\n                Redis[(\"ElastiCache Redis\")]\n                VPCEndpoints[\"VPC Endpoints\"]\n            end\n            \n            subgraph IoTEdge[\"IoT Edge & Telemetry Ingestion\"]\n                IoTCore[\"AWS IoT Core MQTT Broker\"]\n                RuleEngine[\"IoT Rules Engine\"]\n                Firehose[\"Kinesis Data Firehose\"]\n                Timestream[(\"Amazon Timestream\")]\n                S3[(\"Amazon S3 Data Lake\")]\n            end\n            \n            ALB --> ECS\n            ECS --> Aurora\n            ECS --> Redis\n            ECS --> VPCEndpoints\n            \n            IoTCore --> RuleEngine\n            RuleEngine --> Timestream\n            RuleEngine --> Firehose\n            Firehose --> Lambda\n            Lambda --> Firehose\n            Firehose --> S3\n        end\n    end\n                        </div>\n                    </div>\n                </section>\n\n                <section className=\"prose-section\" id=\"section-3\">\n                    <div className=\"section-marker\">03</div>\n                    <div className=\"section-content\">\n                        <p className=\"mini-label\">03 / Ingestion</p>\n                        <h3>IoT Telemetry Ingestion and Edge Connectivity</h3>\n                        <p>Ingesting millions of small payload records (typically under 4 KB) per minute requires a pipeline capable of absorbing severe backpressure and sudden traffic spikes. The architecture segregates command-and-control operations from read-only telemetry by enforcing strict MQTT topic namespaces. Telemetry data is published to prefixes such as <code>data/device/sensortype</code>, while control plane operations use the <code>command/</code> prefix.</p>\n\n                        <h4>Managing Kinesis Data Firehose Backpressure</h4>\n                        <p>The AWS IoT Rules Engine continuously intercepts MQTT messages and routes them to Amazon Kinesis Data Firehose for batching and delivery to an Amazon S3 data lake. Because energy optimization models require rich context, a data transformation AWS Lambda function is configured within the Firehose delivery stream to enrich incoming payloads (e.g., attaching facility geolocation or hardware metadata retrieved from DynamoDB).</p>\n                        <p>Operational disruptions frequently manifest as throttling within this pipeline. If the data source involves a standard Kinesis Data Stream upstream of Firehose, the pipeline is bound by a hard limit of five transactions per second (TPS) on the GetRecords API. Exceeding this limit yields <code>ThrottledGetRecords</code> exceptions, which can result in data loss if the retention window is exceeded.</p>\n                        <p>To mitigate delivery failures, operators must implement the following safeguards:</p>\n                        <ul>\n                            <li><strong>Buffering Hints:</strong> Configure Firehose buffering hints to maximums (e.g., 5 MB or 60 seconds) to reduce the frequency of downstream API calls and optimize the batch sizes written to Amazon S3.</li>\n                            <li><strong>Transformation State Contracts:</strong> The inline Lambda function must strictly adhere to the Firehose response contract, returning a status of <code>Ok</code> for successfully enriched records, <code>Dropped</code> for payloads that fail schema validation, or <code>ProcessingFailed</code> for transient backend errors.</li>\n                            <li><strong>Compute Scaling:</strong> If Firehose delivery falls behind ingestion rates, the root cause is frequently a slow Lambda invocation executing network I/O. The runbook requires increasing the Lambda function's memory allocation, which proportionally increases the allocated CPU threads, allowing the asynchronous I/O operations to process faster.</li>\n                        </ul>\n\n                        <h4>Mitigating Connection Storms and Thundering Herds</h4>\n                        <p>A highly documented failure mode in large-scale IoT fleets is the \"thundering herd\" problem. When a regional internet service provider experiences an outage, millions of smart energy meters may disconnect simultaneously. Upon network restoration, these devices will attempt to reconnect to AWS IoT Core concurrently.</p>\n                        <p>If devices immediately retry failed connections, the synchronized traffic spike acts as a self-inflicted Distributed Denial of Service (DDoS) attack against the cloud infrastructure, overwhelming the IoT Core message broker and custom authorization Lambdas. Furthermore, standard exponential backoff (e.g., waiting 100ms, then 200ms, then 400ms) spaces the traffic out but fails to alter the shape of the spike, creating a series of delayed, synchronized waves.</p>\n                        <p>To achieve true system resilience, remote devices connecting to the cloud must implement exponential backoff combined with jitter. Jitter introduces randomness to the retry delay, explicitly desynchronizing the fleet.</p>\n\n                        <table>\n                            <thead>\n                                <tr>\n                                    <th>Jitter Algorithm</th>\n                                    <th>Logic</th>\n                                    <th>System Impact</th>\n                                </tr>\n                            </thead>\n                            <tbody>\n                                <tr>\n                                    <td><strong>Full Jitter</strong></td>\n                                    <td>Random value between 0 and the current exponential maximum: <code>random(0, min(cap, base * 2^attempt))</code></td>\n                                    <td>Yields the lowest overall workload contention and is the safest approach for large-scale fleets.</td>\n                                </tr>\n                                <tr>\n                                    <td><strong>Equal Jitter</strong></td>\n                                    <td>Holds half the wait fixed and randomizes the remaining half.</td>\n                                    <td>Prevents extremely short sleeps but performs slightly worse than Full Jitter regarding total system load.</td>\n                                </tr>\n                                <tr>\n                                    <td><strong>Decorrelated Jitter</strong></td>\n                                    <td>Ignores the attempt counter, growing the delay based on the previous random sleep value.</td>\n                                    <td>Reaches the maximum delay cap faster, which can delay recovery times for fast-resolving outages.</td>\n                                </tr>\n                            </tbody>\n                        </table>\n\n                        <p>The runbook mandates the deployment of the Full Jitter algorithm across all C-based FreeRTOS edge devices and Python SDKs. Below is the required Python implementation for AWS IoT edge SDKs enforcing exponential backoff with Full Jitter:</p>\n\n                        <pre><code><span className=\"token-keyword\">import</span> time\n<span className=\"token-keyword\">import</span> random\n<span className=\"token-keyword\">from</span> functools <span className=\"token-keyword\">import</span> wraps\n<span className=\"token-keyword\">from</span> botocore.exceptions <span className=\"token-keyword\">import</span> ClientError\n\n<span className=\"token-keyword\">def</span> <span className=\"token-function\">retry_with_full_jitter</span>(max_retries=<span className=\"token-keyword\">5</span>, base_delay=<span className=\"token-keyword\">1</span>, max_delay=<span className=\"token-keyword\">60</span>):\n    <span className=\"token-keyword\">def</span> <span className=\"token-function\">decorator</span>(func):\n        @<span className=\"token-function\">wraps</span>(func)\n        <span className=\"token-keyword\">def</span> <span className=\"token-function\">wrapper</span>(*args, **kwargs):\n            retries = <span className=\"token-keyword\">0</span>\n            <span className=\"token-keyword\">while</span> retries &lt; max_retries:\n                <span className=\"token-keyword\">try</span>:\n                    <span className=\"token-comment\"># Attempt the network operation</span>\n                    <span className=\"token-keyword\">return</span> func(*args, **kwargs)\n                <span className=\"token-keyword\">except</span> ClientError <span className=\"token-keyword\">as</span> e:\n                    error_code = e.response[<span className=\"token-string\">'Error'</span>][<span className=\"token-string\">'Code'</span>]\n                    \n                    <span className=\"token-comment\"># Fail fast for deterministic client errors</span>\n                    <span className=\"token-keyword\">if</span> error_code <span className=\"token-keyword\">in</span> [<span className=\"token-string\">'AccessDeniedException'</span>, <span className=\"token-string\">'ValidationException'</span>]:\n                        <span className=\"token-keyword\">raise</span> e\n                    \n                    <span className=\"token-comment\"># Apply Full Jitter: random(0, min(cap, base * 2^attempt))</span>\n                    exponential_window = <span className=\"token-function\">min</span>(max_delay, base * (<span className=\"token-keyword\">2</span> ** retries))\n                    sleep_time = random.<span className=\"token-function\">uniform</span>(<span className=\"token-keyword\">0</span>, exponential_window)\n                    \n                    time.<span className=\"token-function\">sleep</span>(sleep_time)\n                    retries += <span className=\"token-keyword\">1</span>\n                    \n            <span className=\"token-keyword\">raise</span> Exception(<span className=\"token-string\">\"Maximum retries exhausted due to persistent network failures.\"</span>)\n        <span className=\"token-keyword\">return</span> wrapper\n    <span className=\"token-keyword\">return</span> decorator</code></pre>\n                    </div>\n                </section>\n\n                <section className=\"prose-section\" id=\"section-4\">\n                    <div className=\"section-marker\">04</div>\n                    <div className=\"section-content\">\n                        <p className=\"mini-label\">04 / Security</p>\n                        <h3>Identity and Access Management (IAM): Minimum Privilege Guardrails</h3>\n                        <p>Ensuring a highly secure environment requires layering network perimeter controls with strict, identity-based permissions boundaries. The operations team is responsible for ensuring that any access granted—whether to human operators or machine identities—adheres strictly to the principle of least privilege.</p>\n\n                        <h4>VPC Endpoint Policies</h4>\n                        <p>To protect sensitive operational data, the platform utilizes AWS PrivateLink (VPC Endpoints) to interact with AWS managed services such as Amazon S3, AWS Key Management Service (KMS), and Amazon Timestream. This ensures that traffic never traverses the public internet.</p>\n                        <p>To enforce this architecture programmatically, S3 bucket policies and KMS key policies utilize the <code>aws:SourceVpc</code> condition key. This IAM condition enforces that any successful API call must originate from the specified VPC. Relying on the <code>aws:SourceVpc</code> key is a proven best practice over the <code>aws:SourceVpce</code> (VPC Endpoint ID) key, as endpoint IDs are ephemeral and can change if the infrastructure is redeployed via Terraform or AWS CloudFormation, whereas the VPC ID remains a static identifier.</p>\n                        \n                        <p>An example of a highly restrictive, minimum-privilege S3 bucket policy for the telemetry data lake is provided below:</p>\n                        <pre><code>{\n  <span className=\"token-string\">\"Version\"</span>: <span className=\"token-string\">\"2012-10-17\"</span>,\n  <span className=\"token-string\">\"Statement\"</span>: [\n    {\n      <span className=\"token-string\">\"Sid\"</span>: <span className=\"token-string\">\"EnforceNetworkPerimeter\"</span>,\n      <span className=\"token-string\">\"Effect\"</span>: <span className=\"token-string\">\"Deny\"</span>,\n      <span className=\"token-string\">\"Principal\"</span>: <span className=\"token-string\">\"*\"</span>,\n      <span className=\"token-string\">\"Action\"</span>: <span className=\"token-string\">\"s3:*\"</span>,\n      <span className=\"token-string\">\"Resource\"</span>: [\n        <span className=\"token-string\">\"arn:aws:s3:::energy-optimization-datalake\"</span>,\n        <span className=\"token-string\">\"arn:aws:s3:::energy-optimization-datalake/*\"</span>\n      ],\n      <span className=\"token-string\">\"Condition\"</span>: {\n        <span className=\"token-string\">\"StringNotEquals\"</span>: {\n          <span className=\"token-string\">\"aws:SourceVpc\"</span>: <span className=\"token-string\">\"vpc-0abcd1234efgh5678\"</span>\n        }\n      }\n    },\n    {\n      <span className=\"token-string\">\"Sid\"</span>: <span className=\"token-string\">\"AllowFirehoseDeliveryStream\"</span>,\n      <span className=\"token-string\">\"Effect\"</span>: <span className=\"token-string\">\"Allow\"</span>,\n      <span className=\"token-string\">\"Principal\"</span>: {\n        <span className=\"token-string\">\"Service\"</span>: <span className=\"token-string\">\"firehose.amazonaws.com\"</span>\n      },\n      <span className=\"token-string\">\"Action\"</span>: [\n        <span className=\"token-string\">\"s3:AbortMultipartUpload\"</span>,\n        <span className=\"token-string\">\"s3:GetBucketLocation\"</span>,\n        <span className=\"token-string\">\"s3:GetObject\"</span>,\n        <span className=\"token-string\">\"s3:ListBucket\"</span>,\n        <span className=\"token-string\">\"s3:ListBucketMultipartUploads\"</span>,\n        <span className=\"token-string\">\"s3:PutObject\"</span>\n      ],\n      <span className=\"token-string\">\"Resource\"</span>: [\n        <span className=\"token-string\">\"arn:aws:s3:::energy-optimization-datalake\"</span>,\n        <span className=\"token-string\">\"arn:aws:s3:::energy-optimization-datalake/*\"</span>\n      ]\n    }\n  ]\n}</code></pre>\n\n                        <h4>X.509 Certificate Management and Automated Rotation</h4>\n                        <p>AWS IoT Core enforces Transport Layer Security (TLS 1.3) mutual authentication using X.509 certificates to establish a trusted connection between the edge devices and the cloud broker. These certificates are the core of device identity and are inherently long-lived; however, organizational security policies require scheduled rotation of the certificate and private key to minimize exposure if hardware is physically compromised.</p>\n                        <p>The runbook dictates an automated, over-the-air (OTA) certificate rotation workflow utilizing AWS IoT Device Defender. The platform schedules continuous audit checks, explicitly leveraging <code>DEVICE_CERTIFICATE_EXPIRING_CHECK</code> to identify credentials within 30 days of expiration, and <code>DEVICE_CERTIFICATE_AGE_CHECK</code> to flag certificates exceeding a strict organizational age limit (e.g., 12 months).</p>\n\n                        <h4>IoT Device Defender Anomalous Behavior Detection</h4>\n                        <p>Beyond static certificate audits, the platform employs AWS IoT Device Defender's Machine Learning (ML) and rules-based detect features to monitor for active intrusions.</p>\n                        <table>\n                            <thead>\n                                <tr>\n                                    <th>Metric Target</th>\n                                    <th>IoT Defender Variable</th>\n                                    <th>Operational Implication</th>\n                                </tr>\n                            </thead>\n                            <tbody>\n                                <tr>\n                                    <td><strong>Payload Volume</strong></td>\n                                    <td><code>aws:message-byte-size</code></td>\n                                    <td>Sudden spikes in byte size frequently indicate a compromised device attempting to exfiltrate lateral data or inject malicious shellcode.</td>\n                                </tr>\n                                <tr>\n                                    <td><strong>Authentication Integrity</strong></td>\n                                    <td><code>aws:num-authorization-failures</code></td>\n                                    <td>High concentrations of failures imply that a certificate has been revoked, or an attacker is attempting a brute-force connection utilizing malformed credentials.</td>\n                                </tr>\n                                <tr>\n                                    <td><strong>Connection Stability</strong></td>\n                                    <td><code>aws:num-disconnects</code></td>\n                                    <td>Rapid disconnect/reconnect loops often point to underlying hardware degradation, power source failure, or a synchronized thundering herd scenario.</td>\n                                </tr>\n                            </tbody>\n                        </table>\n                    </div>\n                </section>\n\n                <section className=\"prose-section\" id=\"section-5\">\n                    <div className=\"section-marker\">05</div>\n                    <div className=\"section-content\">\n                        <p className=\"mini-label\">05 / Containers</p>\n                        <h3>Application Deployment and Container Orchestration</h3>\n                        <p>The core API layers of the energy optimization platform are executed as stateless, containerized applications managed by Amazon Elastic Container Service (ECS) utilizing the AWS Fargate serverless compute engine. Fargate abstracts away the underlying EC2 instances, offloading operating system patching and host security to AWS.</p>\n\n                        <h4>OpenTelemetry Sidecar Configuration</h4>\n                        <p>Comprehensive observability is non-negotiable for SRE teams tasked with monitoring a distributed microservice architecture. To facilitate distributed tracing and high-resolution metric collection, an AWS Distro for OpenTelemetry (ADOT) sidecar container must be deployed within every ECS Task Definition.</p>\n                        <p>The following JSON snippet illustrates the proper structure for injecting the ADOT sidecar into an ECS Fargate Task Definition:</p>\n                        <pre><code>{\n  <span className=\"token-string\">\"family\"</span>: <span className=\"token-string\">\"energy-platform-api\"</span>,\n  <span className=\"token-string\">\"requiresCompatibilities\"</span>: [<span className=\"token-string\">\"FARGATE\"</span>],\n  <span className=\"token-string\">\"networkMode\"</span>: <span className=\"token-string\">\"awsvpc\"</span>,\n  <span className=\"token-string\">\"cpu\"</span>: <span className=\"token-string\">\"1024\"</span>,\n  <span className=\"token-string\">\"memory\"</span>: <span className=\"token-string\">\"2048\"</span>,\n  <span className=\"token-string\">\"containerDefinitions\"</span>: [\n    {\n      <span className=\"token-string\">\"name\"</span>: <span className=\"token-string\">\"primary-api-container\"</span>,\n      <span className=\"token-string\">\"image\"</span>: <span className=\"token-string\">\"123456789012.dkr.ecr.us-east-1.amazonaws.com/energy-api:v2.1.0\"</span>,\n      <span className=\"token-string\">\"essential\"</span>: <span className=\"token-keyword\">true</span>,\n      <span className=\"token-string\">\"portMappings\"</span>: [\n        {\n          <span className=\"token-string\">\"containerPort\"</span>: <span className=\"token-keyword\">8080</span>,\n          <span className=\"token-string\">\"protocol\"</span>: <span className=\"token-string\">\"tcp\"</span>\n        }\n      ]\n    },\n    {\n      <span className=\"token-string\">\"name\"</span>: <span className=\"token-string\">\"aws-otel-collector-sidecar\"</span>,\n      <span className=\"token-string\">\"image\"</span>: <span className=\"token-string\">\"public.ecr.aws/aws-observability/aws-otel-collector:latest\"</span>,\n      <span className=\"token-string\">\"essential\"</span>: <span className=\"token-keyword\">true</span>,\n      <span className=\"token-string\">\"command\"</span>: [\n        <span className=\"token-string\">\"--config=/etc/ecs/ecs-default-config.yaml\"</span>\n      ],\n      <span className=\"token-string\">\"environment\"</span>: [\n        {\n          <span className=\"token-string\">\"name\"</span>: <span className=\"token-string\">\"AOT_CONFIG_CONTENT\"</span>,\n          <span className=\"token-string\">\"valueFrom\"</span>: <span className=\"token-string\">\"arn:aws:ssm:us-east-1:123456789012:parameter/otel-collector-config-production\"</span>\n        }\n      ]\n    }\n  ]\n}</code></pre>\n\n                        <h4>ECS Rolling Update Mathematics</h4>\n                        <p>When updating the container images or configuration, ECS relies on the ECS rolling update deployment type. The safety, speed, and capacity impact of a rolling deployment are dictated entirely by two mathematical parameters: <code>minimumHealthyPercent</code> and <code>maximumPercent</code>.</p>\n                        <table>\n                            <thead>\n                                <tr>\n                                    <th>Deployment Scenario</th>\n                                    <th>minimumHealthyPercent</th>\n                                    <th>maximumPercent</th>\n                                    <th>Operational Impact</th>\n                                </tr>\n                            </thead>\n                            <tbody>\n                                <tr>\n                                    <td><strong>Mission-Critical (Zero-Capacity Drop)</strong></td>\n                                    <td>100%</td>\n                                    <td>200%</td>\n                                    <td>Ensures the platform never dips below optimal capacity. The scheduler starts replacement tasks up to double the desired count before terminating any older tasks.</td>\n                                </tr>\n                                <tr>\n                                    <td><strong>Resource-Constrained Cluster</strong></td>\n                                    <td>50%</td>\n                                    <td>100%</td>\n                                    <td>Forces ECS to terminate older tasks to make room for newer ones. Operates at a maximum of the desired count, meaning the application will experience a temporary 50% capacity reduction.</td>\n                                </tr>\n                                <tr>\n                                    <td><strong>Cost-Sensitive Hybrid</strong></td>\n                                    <td>50%</td>\n                                    <td>150%</td>\n                                    <td>Provides a balanced approach, allowing ECS to fluctuate slightly above and below the baseline capacity, mitigating severe outages while saving on compute overhead.</td>\n                                </tr>\n                            </tbody>\n                        </table>\n\n                        <h4>Automated Rollback Procedures and Circuit Breakers</h4>\n                        <p>In modern cloud architecture, human intervention during a failed deployment is an anti-pattern. Manual remediation increases Mean Time to Recovery (MTTR) and introduces catastrophic human error. The runbook requires that all deployments utilize automated circuit breakers to detect failures and revert state instantly.</p>\n                        <p>To solve this, ECS natively integrates with Amazon CloudWatch alarms. The operations team provisions an alarm monitoring the Application Load Balancer's <code>TargetResponseTime</code> and <code>HTTPCode_Target_5XX_Count</code> metrics. If the response time breaches an acceptable baseline at any point during the deployment rollout window, the alarm transitions from <code>OK</code> to <code>ALARM</code>.</p>\n\n                        <p>The AWS CLI command used by the CI/CD pipeline to initiate this highly resilient deployment mechanism is explicitly detailed below:</p>\n                        <pre><code>aws ecs update-service \\\n  --cluster energy-platform-cluster \\\n  --service telemetry-ingestion-api \\\n  --task-definition energy-api:v2 \\\n  --deployment-configuration <span className=\"token-string\">\"maximumPercent=200,minimumHealthyPercent=100,deploymentCircuitBreaker={enable=true,rollback=true},alarms={alarmNames=[HighResponseLatencyAlarm, API5xxErrorAlarm],enable=true,rollback=true}\"</span></code></pre>\n                    </div>\n                </section>\n\n                <section className=\"prose-section\" id=\"section-6\">\n                    <div className=\"section-marker\">06</div>\n                    <div className=\"section-content\">\n                        <p className=\"mini-label\">06 / DB Migrations</p>\n                        <h3>Zero-Downtime Database Schema Migrations</h3>\n                        <p>Modifying relational database schemas in production is historically the highest-risk operation in software engineering. A poorly executed migration can lock tables entirely, backing up incoming API requests, exhausting database connection pools, and effectively halting the energy optimization platform.</p>\n                        \n                        <table>\n                            <thead>\n                                <tr>\n                                    <th>Feature</th>\n                                    <th>Flyway</th>\n                                    <th>Liquibase</th>\n                                </tr>\n                            </thead>\n                            <tbody>\n                                <tr>\n                                    <td><strong>Migration Format</strong></td>\n                                    <td>Raw SQL files (V1__Add_Column.sql) or Java code.</td>\n                                    <td>SQL, XML, YAML, JSON abstractions.</td>\n                                </tr>\n                                <tr>\n                                    <td><strong>Rollback Strategy</strong></td>\n                                    <td>Requires manually written undo scripts; automatic rollbacks are gated behind commercial tiers.</td>\n                                    <td>Offers auto-generated rollbacks for many built-in structural change types on the free tier.</td>\n                                </tr>\n                                <tr>\n                                    <td><strong>State Tracking Ledger</strong></td>\n                                    <td>Uses the <code>flyway_schema_history</code> table.</td>\n                                    <td>Uses the <code>databasechangelog</code> table.</td>\n                                </tr>\n                                <tr>\n                                    <td><strong>Ideal Operational Fit</strong></td>\n                                    <td>Teams managing a single database engine that prioritize raw SQL visibility and simplicity.</td>\n                                    <td>Teams requiring database-agnostic code across multiple environments.</td>\n                                </tr>\n                            </tbody>\n                        </table>\n\n                        <h4>The Expand-Contract (Parallel Change) Pattern</h4>\n                        <p>To achieve true zero-downtime database changes, all migrations must adhere to the Expand-Contract pattern. This methodology breaks down potentially destructive schema changes (such as renaming or splitting a column) into non-breaking, multi-phase deployments, explicitly preventing the need for heavy exclusive table locks that block PostgreSQL read/write operations.</p>\n                        \n                        <p><strong>Phase 1: Expand (Additive Schema Change)</strong></p>\n                        <pre><code><span className=\"token-keyword\">ALTER TABLE</span> devices <span className=\"token-keyword\">ADD COLUMN</span> city <span className=\"token-type\">VARCHAR</span>(<span className=\"token-keyword\">100</span>);\n<span className=\"token-keyword\">ALTER TABLE</span> devices <span className=\"token-keyword\">ADD COLUMN</span> zip_code <span className=\"token-type\">VARCHAR</span>(<span className=\"token-keyword\">20</span>);</code></pre>\n\n                        <p><strong>Phase 2: Migrate (Asynchronous Data Backfill)</strong></p>\n                        <pre><code><span className=\"token-keyword\">UPDATE</span> devices \n<span className=\"token-keyword\">SET</span> city = split_part(device_address, <span className=\"token-string\">','</span>, <span className=\"token-keyword\">1</span>), \n    zip_code = split_part(device_address, <span className=\"token-string\">','</span>, <span className=\"token-keyword\">2</span>) \n<span className=\"token-keyword\">WHERE</span> city <span className=\"token-keyword\">IS NULL</span>;</code></pre>\n\n                        <p><strong>Phase 3: Contract (Cleanup and Deprecation)</strong></p>\n                        <pre><code><span className=\"token-keyword\">ALTER TABLE</span> devices <span className=\"token-keyword\">DROP COLUMN</span> device_address;</code></pre>\n\n                        <p>A critical operational note: when deploying new indexes to the production PostgreSQL tables, the <code>CONCURRENTLY</code> keyword is absolutely mandatory. Executing a standard <code>CREATE INDEX</code> without this keyword immediately places an exclusive lock on the table, blocking all incoming INSERT and UPDATE statements from the IoT telemetry pipeline until the index finishes building.</p>\n                    </div>\n                </section>\n\n                <section className=\"prose-section\" id=\"section-7\">\n                    <div className=\"section-marker\">07</div>\n                    <div className=\"section-content\">\n                        <p className=\"mini-label\">07 / Recovery</p>\n                        <h3>Disaster Recovery and Data Resilience</h3>\n                        <p>Disaster Recovery (DR) in the AWS Cloud is quantified through two overarching business metrics: the Recovery Time Objective (RTO) and the Recovery Point Objective (RPO).</p>\n                        <ul>\n                            <li><strong>Recovery Time Objective (RTO):</strong> Denotes the maximum tolerable duration the application can remain offline following a catastrophic event. It defines the speed of the recovery process.</li>\n                            <li><strong>Recovery Point Objective (RPO):</strong> Denotes the maximum acceptable loss of data, measured backward in time from the point of failure. It defines the necessary frequency of database backups and replication.</li>\n                        </ul>\n                        <p>To support mission-critical energy optimization grids, the RPO target is strictly defined as under 5 minutes, and the RTO is set at a maximum of 15 minutes. To achieve this level of resilience, the platform leverages Amazon Aurora Serverless v2 configured as an Aurora Global Database.</p>\n                        <p>In the event of a total regional outage in the primary deployment (e.g., us-east-1), the runbook dictates promoting the secondary read replica (e.g., us-west-2) to act as the primary cluster. Because Aurora Serverless v2 decouples compute from storage, the secondary region can scale its compute heads instantly to absorb the rerouted traffic, meeting the strict 15-minute RTO. Concurrently, Amazon Route 53 health checks detect the regional failure and seamlessly redirect incoming API and IoT MQTT traffic to the surviving infrastructure.</p>\n                        <p>By enforcing rigorous CI/CD practices, robust telemetry pipelines, minimum-privilege IAM controls, and fully automated deployment rollbacks, this operations runbook ensures that the AWS infrastructure remains exceptionally resilient against both external malicious vectors and internal operational errors.</p>\n                    </div>\n                </section>\n            </div>\n        ";
  const toc = "\n                <a href=\"#section-1\" className=\"is-active\"><span>01</span><strong>Introduction</strong></a>\n                <a href=\"#section-2\"><span>02</span><strong>Architecture & Network</strong></a>\n                <a href=\"#section-3\"><span>03</span><strong>IoT Ingestion</strong></a>\n                <a href=\"#section-4\"><span>04</span><strong>IAM & Security</strong></a>\n                <a href=\"#section-5\"><span>05</span><strong>Container Deployment</strong></a>\n                <a href=\"#section-6\"><span>06</span><strong>Database Migrations</strong></a>\n                <a href=\"#section-7\"><span>07</span><strong>Disaster Recovery</strong></a>\n            ";
  return <ContentWrapper content={content} toc={toc} slug="aws" />;
}
